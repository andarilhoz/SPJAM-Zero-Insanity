///Ataque especial with lasers


if(instance_exists(Jogador)){
    var dis = point_distance(x,y,Jogador.x,Jogador.y-20);
    var dir = point_direction(x,y, Jogador.x,Jogador.y);    
    image_speed = 0;
   // prepara = true;
    
    
}

